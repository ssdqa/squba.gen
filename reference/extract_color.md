# Function to extract colors as hex codes

Function to extract colors as hex codes

## Usage

``` r
extract_color(...)
```

## Arguments

- ...:

  Character names of squba_colors_standard

## Value

name and hex code/s for specified color example useage: extract_color()
(returns all colors) or extract_color("rust") (just returns "rust")
